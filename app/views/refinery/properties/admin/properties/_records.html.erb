<%= render 'refinery/admin/search_header', :url => refinery.properties_admin_properties_path %>
<div class='pagination_container'>
  <% if @properties.any? %>
    <%= render 'properties' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
